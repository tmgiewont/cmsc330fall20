Class DayCareSystem
    def initialize
        @b_hash = Hash.new()
    end

    def loadBabyFromFile(file)
        lines = file.read(file)
        if(lines[0] ~= /^Baby Name: ((\w))


/^[A-Z]+\w* ([A-Z]+\w*) ([0-2][0-9]{3})$/

@
def addClass 
    IO.foreach("data.txt") do |line|
